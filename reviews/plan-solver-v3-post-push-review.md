## Review

- Correct: `PlanSolver.solve/1` now routes through the intended v3 pipeline: `Input.normalize_and_validate`, `PacePolicy.for`, style solver, `Execution.build`, `Validator`, `Apply`, and `Solution` (`lib/burpee_trainer/plan_solver.ex:51` `{:ok, input} <- Input.normalize_and_validate(raw_input),`; `:54` `{:ok, prescription} <- solve_style(input, policy),`; `:55` `execution = Execution.build(prescription),`; `:57` `:ok <- Validator.validate_execution(input, prescription, execution),`; `:58` `{:ok, plan} <- Apply.from_execution(input, execution, prescription),`). Existing saved plans are opened without automatic regeneration (`lib/burpee_trainer_web/live/plans_live/edit.ex:107` `# When editing an existing plan: use its blocks directly.`; `:113` `changeset = Workouts.change_plan(plan)`).
- Correct: Focused existing tests pass: `MIX_ENV=test mix test test/burpee_trainer/plan_solver_test.exs test/burpee_trainer/plan_solver/even_solver_test.exs test/burpee_trainer/plan_solver/unbroken_solver_test.exs test/burpee_trainer/plan_solver/validator_test.exs` => 26 passed; `MIX_ENV=test mix test test/burpee_trainer_web/live/workouts_live_test.exs` => 43 passed.

- Blocker: Critical — even plans silently ignore user-requested additional rests and accept no-boundary leftover rests.
  - Reference: `lib/burpee_trainer/plan_solver/even_solver.ex:9` `explicit_rest_total = Enum.reduce(input.explicit_rests || [], 0, &(&1.duration_sec + &2))`; `lib/burpee_trainer/plan_solver/execution.ex:87` `cadence_sec = prescription.target_duration_sec / prescription.burpee_count`; `lib/burpee_trainer/plan_solver/execution.ex:105` `{elapsed + duration_sec, [event | events]}`.
  - What is wrong: `EvenSolver` subtracts explicit rest time, but `Execution.build/1` for `:even` never emits explicit rest events and ignores `prescription.cadence_sec`. Verified with a one-rep even input plus `%{target_min: 1, rest_sec: 10}`: `PlanSolver.solve/1` returned `{:ok, ...}` with `execution: [%SetEvent{duration_sec: 60.0}]`, `plan.additional_rests` set, and no rest step.
  - Why it matters: User-requested rests can be stored but not executed; one-rep/no-boundary leftover rests return success instead of structured infeasible output.
  - Smallest safe fix: Make even prescriptions place explicit rests on real set boundaries (or return `:cannot_place_explicit_rest`), include them in `Prescription.recoveries`, and have `Execution.build/1` use `prescription.cadence_sec` plus recoveries rather than recomputing `target_duration_sec / burpee_count`.

- Blocker: Important — active coach/catch-up generated plans persist legacy `deterministic-v2` metadata after using the v3 solver.
  - Reference: `lib/burpee_trainer_web/live/overview_live.ex:315` `"solver_version" => "deterministic-v2",`; `lib/burpee_trainer_web/live/overview_live.ex:342` `"solver_version" => "deterministic-v2",`; `lib/burpee_trainer/plan_presentation.ex:19` `if metadata_value(metadata, :solver_version) == 3 and`.
  - What is wrong: `create_coach_plan/3` and `create_catch_up_plans/2` call `PlanSolver.solve/1`, then overwrite the persisted `plan_solver_metadata` with a hand-built legacy version string.
  - Why it matters: This leaves legacy metadata in an active generated-plan path and prevents `PlanPresentation` from using canonical v3 prescription metadata.
  - Smallest safe fix: Merge source/rationale fields into `solution.plan.plan_solver_metadata` and keep `solver_version: 3` instead of replacing solver metadata.

- Blocker: Important — generated v3 solver metadata is dropped when saving plans from the editor and helper API.
  - Reference: `lib/burpee_trainer_web/live/plans_live/edit.ex:196` `defp plan_to_attrs(%WorkoutPlan{} = plan) do`; `lib/burpee_trainer_web/live/plans_live/edit.ex:205` `"blocks" => blocks_to_attrs(plan.blocks),`; `lib/burpee_trainer_web/live/plans_live/edit.ex:969` `full_params =`; `lib/burpee_trainer/workouts.ex:587` `def save_generated_plan(%User{} = user, %WorkoutPlan{} = plan) do`; `lib/burpee_trainer/workouts.ex:598` `"steps" => save_generated_plan_steps(plan.steps)`.
  - What is wrong: Neither `plan_to_attrs/1` nor `Workouts.save_generated_plan/2` carries `plan.plan_solver_metadata` into attrs.
  - Why it matters: Saved generated plans lose `solver_version: 3`, `structure_key`, and prescription blocks, so later UI/explanation falls back to inferred persisted atoms and cannot reliably use the v3 outline path.
  - Smallest safe fix: Add `"plan_solver_metadata" => plan.plan_solver_metadata` to both attr builders and add save/open regression tests that assert `solver_version == 3` remains persisted.

- Blocker: Important — the v3 metadata outline path omits explicit rest steps.
  - Reference: `lib/burpee_trainer/plan_presentation.ex:85` `reset_by_set =`; `lib/burpee_trainer/plan_presentation.ex:87` `|> metadata_value(:auto_resets)`; `lib/burpee_trainer/plan_presentation.ex:109` `recovery_sec =`; `lib/burpee_trainer/plan_presentation.ex:112` `Map.has_key?(reset_by_set, set_index) -> Map.fetch!(reset_by_set, set_index)`; fallback step handling exists at `lib/burpee_trainer/plan_presentation.ex:157` `%PlanStep{kind: :rest, rest_sec: rest_sec}, {sets, next_index} ->`.
  - What is wrong: `outline_from_prescription_metadata/2` reconstructs recoveries only from normal recovery and `auto_resets`; it does not fold persisted `PlanStep{kind: :rest}` or explicit rest metadata into the set rows. Verified with an unbroken v3 solution containing a 30s explicit rest: `sol.plan.steps` had a `kind: :rest` step, while `PlanPresentation.outline(sol.plan)` showed only normal and auto-reset recoveries.
  - Why it matters: Once v3 metadata is actually persisted, the workout outline can hide user-requested additional rests while showing automatic reset recoveries.
  - Smallest safe fix: In the metadata outline path, merge explicit rest steps/additional-rest placements into the expanded set atoms (or persist explicit rest placements in metadata) before `range_rows/1`.

- Blocker: Important — unbroken scoring can choose tiny 1-rep remainder sets even when a balanced partition exists.
  - Reference: `lib/burpee_trainer/plan_solver/structure_search.ex:144` `Enum.flat_map(max_unbroken_reps..1//-1, fn reps ->`; `lib/burpee_trainer/plan_solver/recovery_search.ex:184` `tiny_penalty = if Enum.min(set_pattern) < max(input.max_unbroken_reps - 3, 1), do: 10, else: 0`; `lib/burpee_trainer/plan_solver/candidate_score.ex:11` `candidate.reset_count_miss,`; `lib/burpee_trainer/plan_solver/candidate_score.ex:13` `candidate.structure_shape_penalty,`; spec: `plan_solver_v3_spec.md:516` `- no tiny remainder set`; `plan_solver_v3_spec.md:1060` `- tiny remainder set when a balanced partition exists`.
  - What is wrong: A 20-minute six-count unbroken plan with 120 reps/max 8 selected `structure_key: "12x[8]|2x[8]|1x[5]|3x[1]"` in verification, even though `20x[6]` is generated and balanced.
  - Why it matters: This violates the human-shaped/readable prescription requirement and the explicit regression target for tiny remainder sets.
  - Smallest safe fix: Filter generated structures below the readable minimum when a balanced exact structure exists, or make tiny-set penalties dominate reset-window/pace tie-breaks; add a regression test for 120 reps / 20 min / max 8 asserting no set below the readable floor.

- Note: I found no active `solve_milp_candidates` or `CandidateSearch` references in `lib/`; generic `BurpeeTrainer.Milp.*` remains present but not used by the active `PlanSolver.solve/1` path.
